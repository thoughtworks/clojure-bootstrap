(ns rest-crud-demo.core-test
  (:require [clojure.test :refer :all]
            [rest-crud-demo.core :refer :all]))

(deftest a-test
  (testing "Nothing really. Just a placeholder."
    (is (= 1 1))))
